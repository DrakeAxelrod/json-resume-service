/** @type {import('next').NextConfig} */

// const DEV = process.env.NODE_ENV !== 'production';

module.exports = {
  // output: 'export',
  // assetPrefix: './',
  // reactStrictMode: false,
  // env: { }
}
